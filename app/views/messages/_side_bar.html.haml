.side-bar
  .header
    .header__zone
      .header__zone__name
        = current_user.name
      .header__zone__icon
        .header__zone__icon__left-icon
          = link_to "#" do
            = icon("far", "edit", class: "edit-btn")
        .header__zone__icon__right-icon
          = link_to edit_user_path(current_user) do
            = icon("fas", "cog", class: "cog-btn")

  .groups
    .groups__name
      techcamp
    .groups__message
      No Message!!
    .groups__name
      techcamp
    .groups__message
      まだメッセージはありません
    .groups__name
      techcamp
    .groups__message
      メッセージが届きました!!